import styled from 'styled-components'

export const BlockStepperWrapper = styled.li`
  display: flex;
  flex-direction: column;
  width: 100%;
  gap: 24px;
  counter-reset: stepper;
  list-style-type: none;
`
