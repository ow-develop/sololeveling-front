import styled from 'styled-components'

export const PreparingWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: inherit;
  height: inherit;
  width: 100%;
  min-height: 300px;
`

export const PreparingContent = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  width: 300px;
`

export const PreparingImage = styled.div`
  margin: 0 auto 24px;
`

export const PreparingDesc = styled.p`
  margin-bottom: 24px;
  font-weight: 500;
  font-size: 12px;
  line-height: 18px;
  color: var(--text-weak);
  text-align: center;
  white-space: normal;
`
