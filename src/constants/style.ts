export const HEADER_HEIGHT = 64
export const FOOTER_HEIGHT = 196

export const INNER_HEIGHT = 130

export const LOOP_DELAY = 3000

export const enum Breakpoint {
  MINIMUM = 360,
  S = 480,
  M = 768,
  L = 1024,
  XL = 1200,
  XXL = 1920,
  MAXIMUM = 2560
}

export const enum ZIndex {
  BACKGROUND = -1,
  DEFAULT = 1,
  TOGGLE = 100,
  HOVER = 200,
  DROPDOWN = 400,
  STICKY = 500,
  DRAWER = 600,
  TOAST = 700,
  OVERLAY = 800,
  MODAL = 900
}
